
Contains implementations for `dict.h` using various data structures.

## Usage
Run `make` in the root directory to generate:
- `main_lnode.exe` (Singly linked list)
- `main_list.exe` (Doubly linked list)
- `main_bstree.exe` (Binary Search Tree)

Data files are in `dict-students/`.
